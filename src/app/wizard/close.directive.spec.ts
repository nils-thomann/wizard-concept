import { CloseDirective } from './close.directive';

describe('CloseDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseDirective();
    expect(directive).toBeTruthy();
  });
});
