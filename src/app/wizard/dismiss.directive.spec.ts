import { DismissDirective } from './dismiss.directive';

describe('DismissDirective', () => {
  it('should create an instance', () => {
    const directive = new DismissDirective();
    expect(directive).toBeTruthy();
  });
});
